---
description: Run validation checks for current work (type checking, linting, tests)
---

# Validate

Follow the workflow instructions in **[validate.prompt.md](../../.ai-assistant/workflows/validate.prompt.md)**.

For quick validation of current work, run scoped checks. For full CI verification before pushing, use `--full` flag.
