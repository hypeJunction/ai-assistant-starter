# Project Commands

> **Generated by:** `/init` workflow
> **Updated:** {{LAST_UPDATED}}

## Development

| Task | Command |
|------|---------|
| Dev server | `{{DEV_COMMAND}}` |
| Build | `{{BUILD_COMMAND}}` |
| Preview | `{{PREVIEW_COMMAND}}` |

## Quality Checks

| Task | Command |
|------|---------|
| Type check | `{{TYPECHECK_COMMAND}}` |
| Lint | `{{LINT_COMMAND}}` |
| Lint fix | `{{LINT_FIX_COMMAND}}` |
| Format | `{{FORMAT_COMMAND}}` |

## Testing

| Task | Command |
|------|---------|
| Run all tests | `{{TEST_COMMAND}}` |
| Run single file | `{{TEST_COMMAND}} path/to/file.spec.ts` |
| Run by pattern | `{{TEST_COMMAND}} --grep "pattern"` |
| Watch mode | `{{TEST_WATCH_COMMAND}}` |
| Coverage | `{{TEST_COVERAGE_COMMAND}}` |

## Dependencies

| Task | Command |
|------|---------|
| Install | `{{INSTALL_COMMAND}}` |
| Add dependency | `{{ADD_DEP_COMMAND}} package` |
| Add dev dependency | `{{ADD_DEV_DEP_COMMAND}} package` |
| Update | `{{UPDATE_COMMAND}}` |

## Git Workflow

| Task | Command |
|------|---------|
| Status | `git status` |
| Diff | `git diff` |
| Branch | `git checkout -b feature/name` |
| Commit | `git commit -m "message"` |

## Quick Reference

```bash
# Full validation before commit
{{TYPECHECK_COMMAND}} && {{LINT_COMMAND}} && {{TEST_COMMAND}}

# Quick check during development
{{TYPECHECK_COMMAND}} && {{LINT_COMMAND}}
```
